# NTI_Project
ML project predicting income level (>50K or ≤50K) using the Adult Census dataset. Includes data cleaning, outlier handling, feature scaling, and encoding, followed by training &amp; evaluating Random Forest, KNN, Decision Tree, and Logistic Regression models with full performance metrics (accuracy, precision, recall, F1).
